import { TileStatusPipe } from './tile-status.pipe';

describe('TileStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new TileStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
