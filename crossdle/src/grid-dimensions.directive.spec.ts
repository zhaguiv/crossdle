import { GridDimensionsDirective } from './grid-dimensions.directive';

describe('GridDimensionsDirective', () => {
  it('should create an instance', () => {
    const directive = new GridDimensionsDirective();
    expect(directive).toBeTruthy();
  });
});
